% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_reporte_mensual.R
\name{generar_reporte_brigadas}
\alias{generar_reporte_brigadas}
\title{Generar reporte de productividad por brigadas (Semanal/Mensual)}
\usage{
generar_reporte_brigadas(
  reporte = c("semanal", "mensual"),
  corte,
  id_proyecto,
  pool,
  bd_completa,
  bd_aux,
  insumos,
  week_start = 6,
  umbral_cortos = 2
)
}
\arguments{
\item{reporte}{Character. Tipo de ventana temporal: \code{"semanal"} o \code{"mensual"}.}

\item{corte}{Date. Fecha de corte del reporte.}

\item{id_proyecto}{Numeric/Integer. ID del proyecto en la base de datos (para cruzar altas/bajas).}

\item{pool}{Objeto de conexión a la base de datos (pool DBI).}

\item{bd_completa}{Tibble. Hechos de actividad operativa (registros limpios).}

\item{bd_aux}{Tibble. Estructura administrativa operativa al corte.}

\item{insumos}{List. Lista de insumos generados por cargar_insumos().}

\item{week_start}{Numeric. Día de inicio de la semana (1 = Lunes, 6 = Sábado, 7 = Domingo). Por defecto \code{6}.}

\item{umbral_cortos}{Numeric. Minutos máximos para considerar un diálogo como "corto". Por defecto \code{2}.}
}
\value{
Lista con tres elementos:
\item{registros}{Tibble con el consolidado final de métricas por vocero/coordinador.}
\item{cortos}{Tibble con la matriz de incidencia de diálogos cortos por fecha.}
\item{rango_fechas}{Vector Date que comprende la ventana temporal analizada.}
}
\description{
Genera un consolidado de productividad operativa en una ventana temporal
(semanal o mensual). Calcula el total de diálogos efectivos, la duración
promedio, el tiempo de trabajo en campo y la incidencia de diálogos cortos.

\strong{Lógica de Negocio (Business Rules):}
\itemize{
\item \strong{Efectividad:} Solo se contabilizan como éxito los registros con desglose
exactamente igual a "Efectivo" (Estándar de calidad interno).
\item \strong{Promedio Diario:} Se evalúa sobre el esfuerzo real. Se calcula dividiendo
el total de diálogos únicamente entre los días que el vocero registró actividad
(días hábiles trabajados).
\item \strong{Diálogos Cortos:} Interacciones cuya duración sea menor o igual al parámetro \code{umbral_cortos}.
\item \strong{Huérfanos Operativos:} Los voceros sin coordinador asignado en el catálogo administrativo
no se eliminan; se agrupan bajo la categoría "SIN ASIGNAR" y disparan una alerta de auditoría.
}
}
