% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_etl.R, R/funciones_aux.R
\name{procesar_pase_lista}
\alias{procesar_pase_lista}
\title{Procesa pases de lista desde \code{Registros} y los normaliza a nivel coordinador–usuario}
\usage{
procesar_pase_lista(id_cuestionario, id_proyecto, pool)

procesar_pase_lista(id_cuestionario, id_proyecto, pool)
}
\arguments{
\item{id_cuestionario}{Identificador del cuestionario.}

\item{id_proyecto}{Identificador del proyecto.}

\item{pool}{Conexión a la base de datos.}

\item{fecha_min}{Fecha mínima (inclusive) para considerar registros, en clase \code{Date}.
Por defecto \code{as.Date("2025-03-03")}.}

\item{vars_globales}{Character vector de variables del JSON que se consideran globales del registro
y deben replicarse por usuario. Por defecto \code{c("observaciones", "finalizar")}.}
}
\value{
Un \code{data.table} en formato ancho a nivel usuario, con columnas mínimas:
\itemize{
\item \code{id}: Id del registro en \code{Registros}
\item \code{fecha}: \code{Date} derivada de \code{FechaInicio}
\item \code{obtener_usuario}: coordinador (normalizado)
\item \code{usuario}: usuario reportado (extraído del sufijo numérico de las claves)
\item columnas adicionales: una por cada variable encontrada en el JSON (prefijo antes del sufijo numérico),
más \code{vars_globales} si existen.
}

Una lista con dos elementos:
\itemize{
\item \code{pase_lista}: Tabla con usuarios que asistieron.
\item \code{sin_coord}: Registros sin usuario coordinador.
}
}
\description{
\code{procesar_pase_lista_largo()} extrae registros de la tabla \code{Registros} para un cuestionario dado,
parsea el JSON contenido en \code{Resultado} (asumiendo estructura \strong{plana} clave–valor),
y lo transforma a un formato tabular que preserva la relación:
\strong{(id de registro, fecha de pase, coordinador, usuario reportado, variables del pase)}.

La función está diseñada para minimizar tiempo y memoria al \strong{evitar} construir un
\code{data.frame} ancho con miles de columnas y luego pivotarlo. En su lugar, primero
construye una tabla \strong{larga} (\code{name}, \code{value}) y sólo al final castea a formato ancho
por usuario (\code{dcast}), lo que reduce copias y asignaciones masivas.

Esta función extrae registros de la base de datos, convierte los resultados JSON a un formato tabular
y realiza diversas transformaciones para estructurar los datos.
}
\details{
\subsection{Fuentes y supuestos}{
\itemize{
\item Fuente: tabla SQL \code{Registros} vía \code{pool} (DBI/dbplyr).
\item Se consulta únicamente: \code{Id}, \code{FechaInicio}, \code{Resultado} (mínimo indispensable).
\item \code{Resultado} debe ser un JSON \strong{plano} (no anidado) del tipo \verb{\{ "key": "value", ... \}}.
Si el JSON no es plano, la función sigue intentando procesar mediante el mismo
mecanismo; sin embargo, estructuras anidadas pueden producir columnas/valores no esperados.
}
}

\subsection{Convenciones de negocio (decisiones explícitas)}{

La función implementa reglas de negocio para interpretar el contenido del JSON:
\enumerate{
\item \strong{Definición de fecha de pase}
\itemize{
\item Se usa \code{FechaInicio} como la fecha oficial del pase.
\item Se convierte a \code{Date} (sin hora) y se guarda como \code{fecha}.
\item Se filtra por \code{fecha >= fecha_min}.
}
\item \strong{Definición de coordinador}
\itemize{
\item El coordinador se toma exclusivamente del campo JSON \code{Obtener_usuario}.
\item Se normaliza con \code{toupper()} y \code{str_squish()} para homogenizar (evita discrepancias por espacios).
\item Si un registro \strong{no} contiene \code{Obtener_usuario} o está vacío, el registro se considera
"sin coordinador" y \strong{no se incluye} en la salida principal (porque no es posible vincularlo).
}
\item \strong{Definición de usuario reportado}
\itemize{
\item Las variables por usuario deben venir con el patrón: \verb{<variable>_<usuario>},
donde \verb{<usuario>} es un sufijo numérico al final del nombre (por ejemplo:
\code{asistencia_6351142907}, \code{viviendas_6351142907}).
\item El usuario se extrae con la expresión regular \verb{(?<=_)[0-9]+$}.
\item Si una clave no termina en \verb{_<digits>}, se considera variable \strong{global} del registro
(no asociada a un usuario específico).
}
\item \strong{Variables globales preservadas}
\itemize{
\item Se preservan explícitamente las variables globales:
\itemize{
\item \code{observaciones}
\item \code{finalizar}
}
\item Estas variables se \strong{replican} a todas las filas de usuarios del mismo \code{id}/\code{fecha}/\code{coordinador}.
}
\item \strong{Múltiples pases de lista del mismo coordinador en el mismo día}
\itemize{
\item \strong{No se colapsan} en un único registro.
\item Cada pase corresponde a un \code{id} distinto y produce filas separadas.
\item La llave natural de la salida es:
\code{id + fecha + obtener_usuario + usuario}.
\item Implicación: si un coordinador registra dos pases el mismo día, aparecerán dos "bloques"
de usuarios (uno por \code{id}). Esto preserva auditoría y trazabilidad.
\item Si posteriormente el negocio requiere un "único pase por día", esa deduplicación debe
hacerse aguas abajo (por ejemplo, eligiendo el \code{id} más reciente por \code{FechaInicio}
o un criterio de completitud). Esta función \strong{no decide} cuál es el "bueno";
sólo preserva todos.
}
\item \strong{Claves duplicadas dentro de un mismo pase (misma combinación id/fecha/coordinador/usuario/variable)}
\itemize{
\item Durante el casteo ancho (\code{dcast}), si existen múltiples valores para la misma variable,
se aplica la regla:
\strong{tomar el primer valor no-NA} en el orden de aparición.
\item Motivación: evitar columnas con listas y mantener una tabla rectangular.
}
\item \strong{Registros con JSON inválido}
\itemize{
\item Si \code{fromJSON()} falla para un registro, ese registro se omite (retorna \code{NULL} en el lapply).
\item Motivación: robustez ante corrupción de datos sin abortar todo el proceso.
}
}
}

\subsection{Rendimiento y memoria}{
\itemize{
\item Estrategia: "long-first" (tabla larga) + \code{data.table::dcast} al final.
\item Reduce asignación masiva vs. construir un ancho con \code{rbindlist(fill = TRUE)} sobre miles de columnas.
}
}
}
\examples{
\dontrun{
# Procesar todos los pases del cuestionario 210 desde 2025-03-03
pl <- procesar_pase_lista_largo(pool, id_cuestionario = 210)

# Ver cuántos pases por coordinador y fecha (auditoría: múltiples pases/día)
library(data.table)
setDT(pl)
pl[, .N, by = .(obtener_usuario, fecha, id)][order(obtener_usuario, fecha)]
}

}
