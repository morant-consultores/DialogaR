% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_etl.R
\name{inferir_coordinador_desde_brigada}
\alias{inferir_coordinador_desde_brigada}
\title{Inferir coordinador desde brigada para voceros sin IdSupervisor en UsuarioLog}
\usage{
inferir_coordinador_desde_brigada(
  insumos,
  cargo_coordinador = "Coordinador de Brigada",
  on_ambiguo = c("warn_skip", "error")
)
}
\arguments{
\item{insumos}{Lista producida por \code{cargar_insumos()}.}

\item{cargo_coordinador}{Valor del campo \code{cargo} que identifica coordinadores
en \code{insumos$cat$usuarios}. Debe coincidir con el valor usado en \code{cargar_insumos()}.}

\item{on_ambiguo}{Qué hacer cuando una brigada tiene más de un coordinador:
\itemize{
\item \code{"warn_skip"} (default): emite warning y deja la fila sin modificar.
\item \code{"error"}: aborta con un error explícito.
}}
}
\value{
\code{insumos} con \code{bd_aux} parcheado donde la inferencia fue unívoca.
}
\description{
Hook auxiliar diseñado para usarse dentro de \code{postprocess_insumos}. Cuando
\code{construir_bd_aux} emite un warning por brigadas sin \code{IdSupervisor} registrado
en la plataforma, esta función intenta poblar los campos de coordinador
(\code{nombre_coordinador}, \code{supervisor}, \code{status_coord}) usando la relación
brigada → coordinador del catálogo de usuarios.

La inferencia es \strong{unívoca} cuando hay exactamente un coordinador activo por
brigada. Si hay más de uno, la fila se deja intacta y se emite un warning
para resolución manual.
}
\details{
Esta función \strong{no reemplaza} la corrección en plataforma. Es una mitigación
explícita y opt-in para proyectos donde se sabe que \code{IdSupervisor} no se
popula correctamente. Debe llamarse en el hook \code{postprocess_insumos} del
script de proyecto:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{cargo_coordinador <- "Jefe de Brigada"   # ajustar por proyecto

cargar_insumos(
  ...,
  cargo_coordinador = cargo_coordinador,
  postprocess_insumos = function(insumos) \{
    inferir_coordinador_desde_brigada(insumos, cargo_coordinador = cargo_coordinador)
  \}
)
}\if{html}{\out{</div>}}
}
\section{Seguridad y Privacidad}{

Nivel de datos: INTERNO (estructura operativa, sin PII de encuestados).
Control ISO 27001: A.8.2 (Clasificación de información).
}

