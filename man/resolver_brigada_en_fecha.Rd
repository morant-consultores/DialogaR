% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_etl.R
\name{resolver_brigada_en_fecha}
\alias{resolver_brigada_en_fecha}
\title{Resolver brigada histórica al momento de cada registro de actividad}
\usage{
resolver_brigada_en_fecha(actividad, usuario_log, usuarios_cat)
}
\arguments{
\item{actividad}{Data frame con al menos las columnas \code{usuario_num} (character)
y \code{fecha} (Date).}

\item{usuario_log}{Data frame producido por \code{cargar_usuario_log()};
disponible en \code{insumos$cat$usuario_log}.}

\item{usuarios_cat}{Data frame producido por \code{cargar_usuarios_cat()};
disponible en \code{insumos$cat$usuarios}. Mapea \code{num} → \code{id_usuario}.}
}
\value{
\code{actividad} con la columna \code{id_brigada} (integer) resuelta
históricamente. Filas sin asignación conocida tendrán \code{NA}.
}
\description{
Enriquece un dataframe de actividad con \code{id_brigada} resuelto \strong{al momento
de cada registro}, en lugar de usar la asignación actual del usuario.

Para cada par \verb{(usuario_num, fecha)} en \code{actividad}, busca en \code{usuario_log}
la última asignación de brigada cuya \code{fecha_evento <= fecha} (LOCF). Esto
permite trazar correctamente cuando un vocero trabajó en distintas brigadas
en períodos diferentes.

Si \code{actividad} ya contiene una columna \code{id_brigada}, es reemplazada.
}
