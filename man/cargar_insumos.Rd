% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_etl.R
\name{cargar_insumos}
\alias{cargar_insumos}
\title{Cargar Insumos Operativos (ETL Central)}
\usage{
cargar_insumos(
  pool,
  id_proyecto,
  corte,
  fuentes_actividad,
  ids_pase_lista = integer(),
  procesador_pl = NULL,
  fecha_min_actividad = NULL,
  filtro_minimo_actividad = NULL,
  normalizador_actividad = NULL,
  postprocess_insumos = NULL,
  cargo_coordinador = "Coordinador de Brigada",
  excluir_pruebas = TRUE,
  patron_pruebas = "prueba"
)
}
\arguments{
\item{pool}{Conexión activa a la base de datos (DBI).}

\item{id_proyecto}{Numeric. ID del proyecto a extraer.}

\item{corte}{Date. Fecha límite de análisis.}

\item{fuentes_actividad}{List. Lista de tablas origen y sus metadatos.
Cada elemento admite un campo opcional \code{pool} con una conexión DBI propia
(por ejemplo, cuando la fuente vive en un servidor distinto al de \code{pool},
como un snapshot); si se omite, se usa el \code{pool} principal.}

\item{ids_pase_lista}{Numeric vector. IDs de los cuestionarios de pase de lista.}

\item{procesador_pl}{Function. Función para parsear el JSON del pase de lista.}

\item{fecha_min_actividad}{Date. Límite inferior de la extracción de actividad.}

\item{filtro_minimo_actividad}{Function. Filtros preliminares en SQL.}

\item{normalizador_actividad}{Function. Ajustes de tipos posteriores a la descarga.}

\item{postprocess_insumos}{Function. Hook (Callback) opcional para inyectar reglas de negocio específicas del proyecto al objeto final.}

\item{cargo_coordinador}{Character. Valor exacto del campo \code{Cargo} en la tabla
\code{Usuarios} que identifica a los coordinadores de brigada. Varía por proyecto
(ej. \code{"Coordinador de Brigada"}, \code{"Coordinador"}, \code{"Supervisor"}).
Por defecto \code{"Coordinador de Brigada"}.}

\item{excluir_pruebas}{Logical. Si \code{TRUE} (por defecto), excluye de \code{bd_aux} y
\code{bd_actividad} las brigadas cuyo nombre coincide con \code{patron_pruebas}, de modo
que no aparezcan en ningún reporte. Si alguna brigada de prueba tiene actividad
(diálogos) registrada, se emite una advertencia clara antes de excluirla.}

\item{patron_pruebas}{Character. Expresión regular (case-insensitive) para
identificar brigadas de prueba por \code{nombre_brigada}. Por defecto \code{"prueba"}.}
}
\value{
Una lista estructurada (\code{insumos}) con los dataframes listos para reporteo.
}
\description{
Orquesta la descarga y consolidación de datos desde la base de datos SQL.
Reconstruye la jerarquía operativa al corte (bd_aux), descarga las sábanas
de actividad y procesa múltiples cuestionarios de pase de lista.
}
