% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_contactos.R
\name{construir_base_contactos}
\alias{construir_base_contactos}
\title{Construir base de contactos con clasificación estratégica opcional}
\usage{
construir_base_contactos(
  personaje,
  partido = NULL,
  bd_completa,
  corte,
  bd_aux,
  na_chr = "-",
  clasificacion = FALSE,
  voceros = NULL,
  brigadas = NULL,
  coordinadores = NULL,
  aux_zonas = NULL
)
}
\arguments{
\item{personaje}{Nombre (sin comillas) de la variable que identifica al
personaje político. La función busca las columnas \verb{conoce_<personaje>}
y \verb{opinion_<personaje>} en \code{bd_completa}; si no las encuentra, cae a las
columnas genéricas \code{personaje_conocimiento} y \code{personaje_opinion}.}

\item{partido}{Nombre (sin comillas) de la columna en \code{bd_completa} que
indica la simpatía de partido del entrevistado. Puede ser \code{NULL}
(predeterminado) cuando no se requiere esa columna ni la clasificación.}

\item{bd_completa}{Data frame con los registros crudos de encuesta. Debe
contener al menos: \code{id}, \code{fecha}, \code{seccion}, \code{usuario_num}, columnas de
nombre (\code{nombre_entrevistado}, \code{apellido_paterno}, \code{apellido_materno}) y
domicilio, y las columnas de conocimiento/opinión del personaje.}

\item{corte}{Fecha de corte (\code{Date} o cadena interpretable por R). Solo se
incluyen registros con \code{fecha <= corte}.}

\item{bd_aux}{Data frame producido por \code{cargar_insumos()$bd_aux} con la
jerarquía vocero → brigada → coordinador resuelta al corte. Columnas
requeridas: \code{vocero}, \code{nombre_vocero}, \code{nombre_coordinador}.}

\item{na_chr}{Cadena usada para rellenar \code{NA} en columnas de carácter.
Predeterminado: \code{"-"}.}

\item{clasificacion}{Lógico. Si \code{TRUE}, añade las columnas \code{grupo} y
\code{categoria} con la clasificación estratégica del contacto. Requiere que
\code{partido} no sea \code{NULL}. Predeterminado: \code{FALSE}.}

\item{voceros, brigadas, coordinadores, aux_zonas}{Obsoletos, eliminados en
v0.3.0. Se conservan únicamente para producir un error explícito si
algún llamador todavía los pasa; usa \code{bd_aux} en su lugar. Deben
omitirse o dejarse en \code{NULL}.}
}
\value{
Data frame con una fila por contacto que tenga correo o celular
registrado. Incluye columnas de identificación, ubicación, datos de
contacto, conocimiento y opinión del personaje, y —cuando
\code{clasificacion = TRUE}— las columnas \code{grupo} (carácter \code{"1"}–\code{"6"}) y
\code{categoria} (etiqueta descriptiva).
}
\description{
Integra los registros de encuesta con la jerarquía operativa resuelta por
\code{cargar_insumos()}, filtra los registros que cuentan con al menos un medio
de contacto (correo o celular) y, opcionalmente, clasifica a cada contacto
según su simpatía de partido y su opinión sobre un personaje político.
}
\section{Seguridad y Privacidad}{

Esta función procesa datos personales de los entrevistados (nombre
completo, domicilio, celular, correo). No persiste datos en disco ni los
transmite a servicios externos. Asegúrese de que los datos de entrada
provengan de fuentes autorizadas y de que los resultados se compartan
únicamente con destinatarios con acceso aprobado.
}

