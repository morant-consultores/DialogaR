% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_reporte_auditoria.R
\name{fetch_auditoria_bot}
\alias{fetch_auditoria_bot}
\title{Obtener auditorías (fuente bot: ResultadoAuditoriaBot)}
\usage{
fetch_auditoria_bot(pool, encuesta_id, fecha_inicio = NULL, fecha_fin = NULL)
}
\arguments{
\item{pool}{Objeto de conexión \code{pool}.}

\item{encuesta_id}{Vector. Identificador(es) de encuesta.}

\item{fecha_inicio}{Fecha. Límite inferior (inclusive), en horario CDMX, para
\code{ResultadoAuditoriaBot.Fecha}. \code{NULL} (por defecto) para no filtrar.}

\item{fecha_fin}{Fecha. Límite superior (inclusive), en horario CDMX, para
\code{ResultadoAuditoriaBot.Fecha}. \code{NULL} (por defecto) para no filtrar.}
}
\value{
Tibble normalizado con columnas \code{RegistroId}, \code{usuario_num}, \code{fecha},
\code{dictamenFinal}, \code{totalEvaluacion}, \code{observaciones}.
}
\description{
Extrae las auditorías automáticas del bot para la(s) encuesta(s) indicada(s), colapsando
re-auditorías del mismo registro (vale la última pasada del bot) y aplicando la corrección
humana de \code{RevisionAuditoriaBot} cuando existe (\code{VeredictoCorregido} reemplaza el veredicto
original del bot para ese registro).

Cuando se indican \code{fecha_inicio}/\code{fecha_fin}, el filtro se empuja a la consulta SQL —
pensado para el reporte semanal, que solo necesita la semana inmediata anterior y no debe
traer el histórico completo de auditorías del bot. Cuando ambos son \code{NULL}, se trae el
histórico completo (uso para reportes que combinan fuentes, ver \code{fuente_auditoria = "combinar"}
en \code{\link{generar_reporte_metricas}}).
}
\keyword{internal}
